# Session Management - Visual Overview & Architecture

## 🎨 UI Layout

### AdminSessionsPage - Sessions Tab

```
┌─────────────────────────────────────────────────────────┐
│  Sessions & Schedules Management                        │
│  Manage sessions and their schedules                    │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 📊 STATS                                                │
├──────────────┬──────────────┬──────────────┬────────────┤
│ Total        │ Active       │ Total        │ Upcoming   │
│ Sessions: 12 │ Sessions: 10 │ Schedules:45 │ Schedules:22│
└──────────────┴──────────────┴──────────────┴────────────┘

┌─────────────────────────────────────────────────────────┐
│ [Sessions] [Schedules] ← TAB NAVIGATION                 │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ FILTERS                                                 │
├─────────────────────────────────────────────────────────┤
│ Search: [_______________]  Status: [All Statuses ▼]    │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ [+ Add New Session] ← FORM TOGGLE BUTTON                │
└─────────────────────────────────────────────────────────┘

┌─ CREATE NEW SESSION FORM ────────────────────────────────┐
│  📝 Create New Session                                   │
│                                                          │
│  Category * ────────────────────────────────────────    │
│  [Yoga ▼]    (Pilates, Strength Training)              │
│                                                          │
│  Description * ─────────────────────────────────────────│
│  [________________]  (min 1 char)                       │
│                                                          │
│  Duration (minutes) *  │  Capacity *                    │
│  [60 (min 15)]         │  [15 (min 1)]                  │
│                                                          │
│  Price ($) *           │  Trainer (Optional)            │
│  [20.00 (min 0)]       │  [Select trainer... ▼]         │
│                                                          │
│  [✓ Create Session]  [Cancel]                           │
│                                                          │
│  ✓ Session created successfully! ✨                     │ ← Success msg
│  (auto-dismisses after 2 seconds)                       │
│                                                          │
│  ⚠️ {error message here}  ← Error display               │
└──────────────────────────────────────────────────────────┘

┌─ SESSIONS TABLE ─────────────────────────────────────────┐
│  Sessions (12)                                           │
│                                                          │
│ Title │ Trainer │ Type │ Capacity │ Status │ Created   │
├───────┼─────────┼──────┼──────────┼────────┼───────────┤
│Yoga   │John Doe │Yoga  │15        │ACTIVE  │12/1/2024  │
│Pilates│Jane Doe │Pilates│20       │ACTIVE  │12/1/2024  │
│...    │...      │...   │...       │...     │...        │
└──────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow

### Creating a Session

```
┌─────────────────┐
│   User clicks   │
│  "+ Add New     │
│   Session"      │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────┐
│  Show Create Session Form   │
│  (showCreateForm = true)    │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│   User fills out:           │
│   • Category                │
│   • Description             │
│   • Duration                │
│   • Capacity                │
│   • Price                   │
│   • Trainer (optional)      │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ User clicks                 │
│ "✓ Create Session"          │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ Frontend Validation         │
│ • Check all required fields │
│ • Validate ranges           │
│ • Validate data types       │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ POST /sessions              │
│ Content-Type: application/json
│ Authorization: Bearer {JWT} │
│ Body: {form data}           │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ Backend Processing          │
│ • JWT verification          │
│ • Role check (admin/manager)│
│ • DTO validation            │
│ • Database insert           │
└────────┬────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Response 201 Created         │
│ + Session object             │
└────────┬─────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ Frontend Updates            │
│ • Add to sessions array     │
│ • Update filtered list      │
│ • Show success message      │
│ • Reset form                │
│ • Auto-close after 2s       │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ User sees new session       │
│ in table instantly! ✅      │
└─────────────────────────────┘
```

---

## 🏗️ Architecture

### Component Structure

```
AdminSessionsPage
├── State Management
│   ├── sessions: Session[]
│   ├── filteredSessions: Session[]
│   ├── showCreateForm: boolean
│   ├── isSubmitting: boolean
│   ├── formData: SessionFormData
│   ├── trainers: Trainer[]
│   ├── formError: string | null
│   └── formSuccess: string | null
│
├── Effects
│   ├── fetchSessions() - Fetch on mount & filter change
│   ├── fetchTrainers() - Fetch trainer list
│   └── updateFilters() - Apply search/filter
│
├── Handlers
│   ├── handleFormChange() - Update form state
│   ├── handleCreateSession() - Submit form
│   └── getStatusColor() - UI helper
│
└── JSX Render
    ├── Header
    ├── Stats Cards
    ├── Tabs (Sessions/Schedules)
    ├── Filters
    ├── Add Session Button
    ├── Create Form (conditional)
    └── Sessions Table
```

### API Integration

```
┌─────────────────────────┐
│   AdminSessionsPage     │
└────────┬────────────────┘
         │
    ┌────┴────┐
    │          │
    ▼          ▼
┌─────────┐ ┌──────────┐
│ GET     │ │ GET      │
│/sessions│ │/trainers │
│(list)   │ │(dropdown)│
└────┬────┘ └────┬─────┘
     │           │
     ▼           ▼
  Sessions    Trainers
   array       array
     │           │
     └─────┬─────┘
           ▼
     ┌───────────┐
     │ Populate  │
     │ Form      │
     └─────┬─────┘
           │
       [Form]
           │
        Submit
           │
           ▼
     ┌──────────────┐
     │POST /sessions│
     │with form data│
     └──────┬───────┘
            │
            ▼
     ┌─────────────┐
     │ Create      │
     │ successful  │
     │ Response    │
     └──────┬──────┘
            │
            ▼
     ┌──────────────┐
     │ Add to state │
     │ Update table │
     │ Show success │
     └──────────────┘
```

---

## 📋 Form State Machine

```
INITIAL STATE
    │
    ├─ showCreateForm = false (hidden)
    ├─ isSubmitting = false
    ├─ formError = null
    ├─ formSuccess = null
    └─ formData = {defaults}

    ↓ (User clicks "+ Add New Session")

FORM_VISIBLE
    │
    ├─ showCreateForm = true (visible)
    ├─ formData = {editable}
    └─ User fills fields

    ↓ (User clicks "✓ Create Session")

SUBMITTING
    │
    ├─ isSubmitting = true (button disabled)
    ├─ Form fields disabled
    └─ Loading spinner on button

    ├─ IF SUCCESS:
    │  ├─ Response received (201)
    │  ├─ Add to sessions array
    │  ├─ formSuccess = "Session created successfully! ✨"
    │  ├─ isSubmitting = false
    │  ├─ Wait 2 seconds
    │  ├─ showCreateForm = false (auto-close)
    │  └─ formSuccess = null (clear message)
    │
    └─ IF ERROR:
       ├─ Response error caught
       ├─ formError = "Error message"
       ├─ isSubmitting = false
       └─ User can retry
```

---

## 🔐 Security Flow

```
User Request
    ↓
┌─────────────────────┐
│ Browser sends JWT   │
│ in Authorization    │
│ header              │
└────────┬────────────┘
         ↓
┌─────────────────────┐
│ Backend receives    │
│ POST /sessions      │
└────────┬────────────┘
         ↓
┌─────────────────────┐
│ @UseGuards          │
│ (JwtAuthGuard)      │
│ ↓ Verify JWT        │
│ ✓ Valid JWT?        │
└────────┬────────────┘
         ↓
┌─────────────────────┐
│ @UseGuards          │
│ (RolesGuard)        │
│ ↓ Extract role      │
│ ✓ admin or manager? │
└────────┬────────────┘
         ↓
┌─────────────────────┐
│ @Roles('admin',     │
│ 'manager')          │
│ ✓ Role matches?     │
└────────┬────────────┘
         ↓
┌─────────────────────┐
│ Create Session      │
│ ✓ Proceed safely    │
└─────────────────────┘
```

---

## 🧪 Form Validation Rules

```
┌─ Description ────────────────────────┐
│ Input: ""                            │
│ Check: !formData.description.trim()  │
│ Error: "Session description required"│
└──────────────────────────────────────┘

┌─ Category ───────────────────────────┐
│ Input: ""                            │
│ Check: !formData.category            │
│ Error: "Category is required"        │
└──────────────────────────────────────┘

┌─ Duration ───────────────────────────┐
│ Input: 10                            │
│ Check: < 15                          │
│ Error: "Duration must be at least15m"│
└──────────────────────────────────────┘

┌─ Capacity ───────────────────────────┐
│ Input: 0                             │
│ Check: < 1                           │
│ Error: "Capacity must be at least 1" │
└──────────────────────────────────────┘

┌─ Price ──────────────────────────────┐
│ Input: -5                            │
│ Check: < 0                           │
│ Error: "Price cannot be negative"    │
└──────────────────────────────────────┘
```

---

## 📊 Data Model

```
Session Object (Response):
{
  session_id: number,          ← Auto-generated
  category: 'yoga'|'pilates'|'strength_training',
  description: string,         ← User input
  duration_minutes: number,    ← User input (min 15)
  capacity: number,            ← User input (min 1)
  price: string (decimal),     ← User input (≥ 0)
  trainer_id?: number,         ← Optional user input
  created_by?: number,         ← System set
  trainer?: {                  ← Populated from DB
    trainer_id: number,
    name: string
  }
}

Form Data (State):
{
  category: string,            ← 'yoga' (default)
  description: string,         ← '' (empty)
  duration_minutes: number,    ← 60 (default)
  capacity: number,            ← 15 (default)
  price: number,               ← 20 (default)
  trainer_id: string | number  ← '' (empty)
}
```

---

## ✅ Implementation Checklist

- [x] Backend: Added 'manager' role to POST endpoint
- [x] Backend: Added 'manager' role to PATCH endpoint
- [x] Backend: Added 'manager' role to DELETE endpoint
- [x] Frontend: Form state management (7 state variables)
- [x] Frontend: Fetch trainers on mount
- [x] Frontend: Form input handlers
- [x] Frontend: Form submission handler
- [x] Frontend: Frontend validation
- [x] Frontend: Error display
- [x] Frontend: Success message
- [x] Frontend: Real-time table update
- [x] Frontend: Form auto-close
- [x] Frontend: Button disabled state during submit
- [x] Security: JWT authentication
- [x] Security: Role-based access
- [x] Security: Input validation
- [x] Testing: All validation paths

---

**Status**: ✅ **COMPLETE AND PRODUCTION-READY**
