# Manager System - Architecture Diagrams

## User Role Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         Registration                             │
└─────────────────────────────────────────────────────────────────┘
                              │
                ┌─────────────┼─────────────┬──────────────┐
                │             │             │              │
            role=          role=          role=         role=
           client         trainer        manager         admin
                │             │             │              │
    ┌───────────▼──┐  ┌──────▼────────┐  ┌▼─────────────┐ ┌▼────────────┐
    │  User Entity │  │ User Entity   │  │User Entity   │ │User Entity  │
    │   (only)     │  │ + Trainer     │  │+ Manager ✨  │ │+ Admin      │
    │              │  │   Entity      │  │   Entity     │ │   Entity    │
    └───────────┬──┘  └──────┬────────┘  └┬─────────────┘ └┬────────────┘
                │             │             │              │
                ▼             ▼             ▼              ▼
          ┌─────────┐  ┌─────────────┐ ┌──────────────┐ ┌──────────────┐
          │ Client  │  │  Trainer    │ │   Manager    │ │     Admin    │
          │Dashboard│  │  Dashboard  │ │   Dashboard  │ │   Dashboard  │
          └─────────┘  └─────────────┘ └──────────────┘ └──────────────┘
```

---

## Database Schema Relationship

```
┌────────────────────┐
│     USER TABLE     │
├────────────────────┤
│ user_id (PK)       │
│ username           │
│ email              │
│ role ◄─────────────┼──── "manager" role
│ status             │
│ password           │
│ created_at         │
│ updated_at         │
└────────────────────┘
         │
         │ One-to-One
         │ Relationship
         │
    ┌────▼─────────────────┐
    │   MANAGER TABLE       │
    ├──────────────────────┤
    │ manager_id (PK)      │
    │ user_id (FK, Unique) │
    │ center_id ◄──────────┼─── Optional: which center
    │ manager_role         │     (center_manager, regional_manager, etc.)
    │ manager_status       │
    │ department           │
    │ trainers_count       │
    │ sessions_count       │
    │ created_at           │
    │ updated_at           │
    └──────────────────────┘
```

---

## Manager Can Manage Trainers

```
┌──────────────────────────────────────────────────────────┐
│                   MANAGER DASHBOARD                      │
│                                                          │
│  Manager: John (center_id = 1)                          │
│  ─────────────────────────────────────────              │
│                                                          │
│  ┌──────────────────────────────────────────────┐       │
│  │ Trainers Assigned to Center 1:               │       │
│  │                                              │       │
│  │ ✓ Trainer 1 (Yoga Specialist)               │       │
│  │ ✓ Trainer 2 (Pilates Specialist)            │       │
│  │ ✓ Trainer 3 (CrossFit Specialist)           │       │
│  │                                              │       │
│  │ [Can edit / reassign / remove trainers]      │       │
│  └──────────────────────────────────────────────┘       │
│                                                          │
│  ┌──────────────────────────────────────────────┐       │
│  │ Sessions for Center 1:                       │       │
│  │                                              │       │
│  │ • Yoga Class (Trainer 1, Mon 10am)          │       │
│  │ • Pilates Class (Trainer 2, Wed 4pm)        │       │
│  │ • CrossFit Class (Trainer 3, Fri 6pm)       │       │
│  │                                              │       │
│  │ [Can view bookings / manage schedules]       │       │
│  └──────────────────────────────────────────────┘       │
│                                                          │
│  Statistics:                                            │
│  • Active Trainers: 3                                   │
│  • Total Sessions: 15                                   │
│  • Bookings This Week: 47                               │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

## Admin vs Manager Permission Levels

```
┌─────────────────────────────────────────┐
│      ADMIN PERMISSION LEVELS             │
│      (in Admin Entity)                   │
│                                          │
│  SUPER_ADMIN                             │
│  ├─ Manage all users                    │
│  ├─ Create/delete admins                │
│  ├─ System configuration                │
│  └─ Full platform access                │
│                                          │
│  ADMIN                                   │
│  ├─ Manage users/trainers               │
│  ├─ Manage bookings/sessions            │
│  ├─ View reports                        │
│  └─ Platform management (not config)    │
│                                          │
└─────────────────────────────────────────┘

           ↕️ SEPARATE ↕️

┌─────────────────────────────────────────┐
│     MANAGER PERMISSION LEVELS            │
│     (in Manager Entity)                  │
│                                          │
│  OPERATIONS_HEAD                         │
│  ├─ Manage all centers                  │
│  ├─ Approve trainers for all centers    │
│  ├─ System-wide scheduling              │
│  └─ Center performance reports          │
│                                          │
│  REGIONAL_MANAGER                        │
│  ├─ Manage multiple centers (region)    │
│  ├─ Approve trainers for region         │
│  ├─ Regional scheduling                 │
│  └─ Regional reporting                  │
│                                          │
│  CENTER_MANAGER                          │
│  ├─ Manage one center                   │
│  ├─ Assign trainers                     │
│  ├─ Schedule sessions                   │
│  └─ View center reports                 │
│                                          │
└─────────────────────────────────────────┘
```

---

## Data Flow: Creating a Manager

```
┌──────────────────┐
│  Admin Console   │
└────────┬─────────┘
         │
         │ Step 1: Create User
         ▼
    ┌────────────────────────────┐
    │  POST /api/users           │
    │ {                          │
    │   username: "manager1",    │
    │   email: "...",            │
    │   role: "manager",  ◄──────── KEY!
    │   password: "..."          │
    │ }                          │
    └────────┬───────────────────┘
             │
             ▼
    ┌────────────────────┐
    │   Users Table      │
    │  ID=5, role=mgr    │
    └────────┬───────────┘
             │
             │ Step 2: Create Manager
             ▼
    ┌────────────────────────────┐
    │  POST /api/managers        │
    │ {                          │
    │   user_id: 5,    ◄─────────── Link to User
    │   center_id: 1,            │
    │   manager_role: "center_manager",
    │   department: "Main Branch"│
    │ }                          │
    └────────┬───────────────────┘
             │
             ▼
    ┌────────────────────┐
    │  Managers Table    │
    │  user_id=5         │
    │  center_id=1       │
    └────────┬───────────┘
             │
             │ Step 3: Manager Logs In
             ▼
    ┌───────────────────────────┐
    │  Manager Dashboard        │
    │  See trainers & sessions  │
    │  for Center 1             │
    └───────────────────────────┘
```

---

## API Security & Access

```
┌─────────────────────────────────────────────────────┐
│              /api/managers ENDPOINTS                 │
└─────────────────────────────────────────────────────┘

ALL ENDPOINTS REQUIRE:

1. Valid JWT Token
   ▼
   ┌──────────────────────────┐
   │ Authorization: Bearer... │
   └──────────────────────────┘

2. User Role = 'admin'
   ▼
   ┌──────────────────────────────────────┐
   │ @UseGuards(JwtAuthGuard, RolesGuard)│
   │ @Roles('admin')                     │
   └──────────────────────────────────────┘

3. Therefore: ONLY ADMINS can create/manage managers

┌──────────────────────────────────────────┐
│ Allowed: Admin Creates Manager Profile   │
│                                          │
│  POST /api/managers                     │
│  Headers: Authorization: Bearer XXX     │
│  Body: { user_id, center_id, ... }      │
│                                          │
│  ✅ Success: Manager created             │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│ Denied: Regular User Tries to Create     │
│                                          │
│  POST /api/managers                     │
│  User Role: 'client' or 'trainer'       │
│                                          │
│  ❌ Error: Insufficient permissions      │
└──────────────────────────────────────────┘
```

---

## File Structure Overview

```
atarabackend/
│
├── src/
│   ├── managers/ ✨ NEW MODULE
│   │   ├── entities/
│   │   │   └── manager.entity.ts         ✨ Entity with enums
│   │   ├── dto/
│   │   │   └── manager.dto.ts            ✨ DTOs
│   │   ├── manager.controller.ts         ✨ Endpoints
│   │   ├── manager.service.ts            ✨ Business logic
│   │   └── manager.module.ts             ✨ Module config
│   │
│   ├── admin/
│   │   ├── entities/
│   │   │   └── admin.entity.ts           (updated enum)
│   │   ├── admin.controller.ts
│   │   ├── admin.service.ts
│   │   └── admin.module.ts
│   │
│   ├── users/
│   ├── trainers/
│   ├── sessions/
│   │
│   └── app.module.ts                     (updated)
│
├── frontend/
│   └── src/pages/
│       ├── AdminDashboard.tsx
│       ├── ManagerDashboard.tsx          ✨ NEW
│       ├── TrainerDashboard.tsx
│       └── ClientDashboard.tsx
│
└── Documentation/
    ├── MANAGER_ENTITY_DOCUMENTATION.md   ✨ Detailed
    ├── MANAGER_SYSTEM_IMPLEMENTATION.md  ✨ Complete guide
    └── MANAGER_QUICK_REFERENCE.md        ✨ Quick ref
```

---

## Summary

```
BEFORE: Admin role handled everything
        (User + Admin Entity)

AFTER:  Four separate dashboard roles
        (Client + Trainer + Manager + Admin)

        ┌─────────────────────────────────┐
        │ Client    → Books sessions      │
        │ Trainer   → Creates sessions    │
        │ Manager   → Manages trainers ✨ │
        │ Admin     → Manages platform    │
        └─────────────────────────────────┘
```

**Each role has their own entity, dashboard, and permissions!** 🎉
